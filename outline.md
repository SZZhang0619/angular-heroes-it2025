# Angular：現代英雄之旅系列

## 第一階段：Angular 基礎與現代語法 (Day 1 - 7)

| 天數 | 主題 | 核心內容規劃 | 個人化切入點 (學徒心聲) | 教授的關鍵特性 (所屬版本) | 融入策略與優化說明 |
| :-- | :--- | :--- | :--- | :--- | :--- |
| **Day 1** | **系列介紹：重建現代英雄之旅** | 介紹系列緣由（官方教程的移除）、我的 Angular 學習歷程。發布「英雄地圖」(30天學習路徑圖)，確立「英雄管理系統」的專案目標。 | 「我還記得第一次踏上『英雄之旅』時的激動。當我發現它消失時，心中滿是失落。這趟旅程，不僅是為了你們，也是為了找回我心中的那份初衷。」 | 系列介紹 | **明確本系列的獨特定位**：強調這不僅是 CRUD 教學，更是「Angular 官方英雄之旅精神的現代再現」，透過故事化、檢查點與挑戰日，提供遊戲化的學習體驗。 |
| **Day 2** | **環境設定：Standalone 專案結構** | 使用最新的 Angular CLI 建立專案。詳解 Standalone 專案結構、`angular.json` 與 `tsconfig.json` 的關鍵設定，並強調 Vite 帶來的極速開發體驗。 | 「想當年光是搞懂 NgModule 就花了我一週... 現在的 `ng new` 就像是新手神裝，一鍵配齊，讓我們能更快專注在冒險本身！」 | 獨立元件 (v19 預設), CLI 整合 Vite/esbuild (v17) | 從第一天就讓讀者體驗到最快速、最現代的開發流程，建立良好第一印象。 |
| **Day 3** | **核心概念：Component 詳解** | 創建 `AppComponent`，深入解析 `@Component` 裝飾器中的 `selector`, `template`, `styles`, `standalone: true`, `imports` 屬性。 | 「看著自己親手寫下的元件，第一次在畫面上亮起，那種魔法般的感動，就是支持我走到今天的動力。」 | 獨立元件 (v15 穩定版) | 直接教授現代 Angular 的架構基石，完全跳過 `NgModule` 的複雜性。 |
| **Day 4** | **範本語法：資料綁定與 `@if` 控制流** | 在元件中定義一個英雄物件，並使用插值 `{{ }}` 顯示資料。介紹並實作新的內建控制流 `@if` 來取代 `*ngIf`。 | 「新的 `@if` 語法不僅更直觀，效能也更好。告別星號(*)的時代，讓我們寫出更優雅的模板！」 | 內建控制流 (v17) | 拋棄舊的結構型指令，直接學習語法更優雅、效能更好的新寫法。 |
| **Day 5** | **範本語法：`@for` 迴圈與 `track`** | 建立英雄列表資料。使用新的 `@for` 迴圈語法來渲染列表，並從一開始就強調 `track` 的重要性與用法。 | 「還記得過去為了 `trackBy` 寫一堆函式嗎？現在 `@for` 內建的 `track` 讓效能優化變得如此簡單，真是新手的福音。」 | 內建控制流 (v17) | 取代 `*ngFor`，並從基礎就強調 `track` 的重要性以優化效能。 |
| **Day 6** | **使用者互動：事件處理與 Signal 初探** | 介紹 `(click)` 事件綁定，讓使用者可以選擇一位英雄。使用 `signal()` 來管理「當前選中的英雄」這個簡單狀態，並將其顯示在畫面上。 | 「第一次讓畫面『聽』我的話，感覺就像學會了第一個魔法！而且這個叫 Signal 的新魔法，用起來意外地簡單直觀。」 | Angular 基礎, Signals (v16) | **提前鋪墊 Signals**，讓讀者首次接觸，感受其直觀性。並明確提醒：「這只是 Signals 的初次亮相，我們將在 Day 21 深入解析其運作原理。」 |
| **Day 7** | **第一週總結：專案檢視與 `ngModel`** | 回顧 Day 1-6 的專案結構與程式碼。並在此基礎上，輕鬆地加入 `ngModel` 範本驅動表單來編輯英雄名稱，作為本階段的收尾練習。 | 「一週的冒險結束了，讓我們圍在營火邊，看看我們的成果。休息是為了走更長遠的路，順便也來學個簡單的表單技巧吧！」 | `FormsModule` in Standalone | **設立第一個學習檢查點**。透過回顧鞏固所學，並用一個簡單的功能作為收尾，增加讀者的成就感與持續下去的動力。 |

## 第二階段：應用程式核心功能 (Day 8 - 18)

| 天數 | 主題 | 核心內容規劃 | 個人化切入點 (學徒心聲) | 教授的關鍵特性 (所屬版本) | 融入策略與優化說明 |
| :-- | :--- | :--- | :--- | :--- | :--- |
| **Day 8** | **架構設計：Service 與依賴注入 (`inject`)** | 建立一個 `HeroService`，將英雄資料從元件中抽離。講解依賴注入的核心概念，並同時介紹 `constructor` 注入和更靈活的 `inject()` 函式。 | 「把所有程式碼塞在同一個地方，就像把所有裝備都丟進一個大袋子。學會服務，就是學會分類打包，讓你的冒險行囊井然有序。」 | `inject()` 函式 (v14) | 教授軟體工程的核心概念「關注點分離」，並介紹現代化的 DI 方式。 |
| **Day 9** | **非同步處理：RxJS Observable 入門** | 將 `HeroService` 的方法改為回傳 `Observable`。解釋為什麼需要非同步，以及 Observable 的基本概念（訂閱）。 | 「真實的世界不會等你準備好才發生。`Observable` 就像是學會預知未來，讓我們能優雅地處理那些『不知道什麼時候會來』的事件。」 | RxJS 基礎 | 為處理非同步事件打下基礎，這是現代 Web 開發的必備技能。 |
| **Day 10**| **路由系統：`provideRouter` 與基礎配置** | 建立 `app.routes.ts`，設定儀表板 (`Dashboard`) 和英雄列表 (`Heroes`) 兩個路由。介紹 `router-outlet`、`routerLink` 與 `provideRouter`。 | 「路由就像是我們冒險地圖上的傳送點，規劃得好，才能讓使用者在我們的世界裡自由穿梭。」 | 獨立路由 API (v15) | 教授完全不需 `NgModule` 的現代路由設定方法。 |
| **Day 11**| **路由系統：路由參數綁定至 `@Input`** | 實現點擊英雄後跳轉到專屬的詳情頁面 (`/detail/:id`)。展示如何直接將路由參數綁定到元件的 `@Input`，取代從 `ActivatedRoute` 手動取得。 | 「新版的路由參數綁定，就像是傳送點直接把寶物送到你手上，再也不用自己去翻箱倒櫃（`ActivatedRoute`）了！」 | 路由參數綁定至輸入 (v16) | 展示如何以更簡潔、更宣告式的方式處理路由參數，提升開發體驗。 |
| **Day 12**| **HTTP 通訊：`HttpClient` 與模擬 API** | 引入 `provideHttpClient()`。使用 `HttpClient` 取代模擬資料，從遠端 API 獲取英雄列表。安裝並設定 `in-memory-web-api` 來模擬後端。 | 「第一次成功串接後端 API，看到真實資料顯示在畫面上時，感覺自己像打通了任督二脈！我們的世界終於和外界連上線了。」 | `provideHttpClient()` (v15) | 教授在獨立應用中設定 `HttpClient` 的標準做法，並提供模擬工具降低學習門檻。 |
| **Day 13**| **CRUD 實作：新增(POST)與更新(PUT)** | 將新增(POST)與更新(PUT)操作放在同一天教學，因為它們通常共用同一個表單或資料模型，邏輯相似度高。 | 「創造與改變，是英雄成長的必經之路。今天，我們將賦予使用者這兩種強大的權力。」 | `HttpClient` (POST, PUT) | **融合篇幅**，將邏輯相似的功能放在一起，加快學習節奏，避免內容重複。 |
| **Day 14**| **CRUD 實作：刪除(DELETE)與錯誤處理** | 將刪除(DELETE)操作與 `catchError` 錯誤處理結合。刪除是一個極佳的場景來演示成功提示與失敗回饋。 | 「有誕生，就有殞落。但作為守護者，即使在執行刪除這樣危險的操作時，我們也要確保系統的穩定，並給予使用者清晰的指引。」 | `HttpClient` (DELETE), RxJS `catchError` | **融合篇幅**，將關聯性強的操作（危險操作與錯誤處理）結合，讓學習場景更真實。 |
| **Day 15**| **UI/UX 優化：Loading 與 Empty 狀態** | 為英雄列表加上 `*ngIf` 判斷，當資料正在載入時顯示「載入中」的提示（或是一個簡單的 Spinner），以及當英雄列表為空時的「空狀態」畫面。 | 「一個好的應用程式，不只在成功時表現完美，更要在等待與空無一物時，給予使用者溫柔的回應。這就是體驗的細節。」 | UI/UX 實踐 | **新增 UX 改善篇**。利用前幾天建立的非同步操作，為應用加入專業的 UI 反饋，提升應用質感。 |
| **Day 16**| **非同步處理：RxJS Operators 應用 (搜尋功能)** | 建立搜尋功能，當使用者輸入時，即時發送搜尋請求。活用 `debounceTime`, `distinctUntilChanged`, `switchMap` 等 RxJS 操作符優化搜尋體驗。 | 「這是我認為英雄之旅最精華的部分之一，它完美展示了 RxJS 的強大之處。學會它，你就掌握了處理複雜事件流的『鷹眼』。」 | RxJS 操作符 | 展示 RxJS 在真實場景中的強大威力，這也是 Angular 開發者必學的核心技能之一。 |
| **Day 17**| **綜合練習：讀者挑戰日** | 不教授新知。提供 2-3 個基於現有進度的小挑戰（例如：1. 新增英雄分類篩選按鈕。2. 為搜尋框加上 `debounceTime`。3. 自行抽出一個共用 UI 指令）。 | 「師父領進門，修行在個人。今天換你們來當主角！別怕犯錯，每一次嘗試都是邁向英雄的一步。」 | 綜合應用 | **設立主動學習日**。鼓勵讀者從「跟隨」轉為「實踐」，鞏固所學並培養解決問題的能力。 |
| **Day 18**| **第二週總結：程式碼重構與 `NgOptimizedImage`** | 全面檢視已完成的 CRUD 功能，整理 `HeroService` 中的程式碼。並將 `NgOptimizedImage` 作為效能優化的一環在此引入。同時提供前一天挑戰的解答思路。 | 「我們的程式碼就像是英雄的武器，需要時常擦拭與保養。好的重構能讓它在未來的戰鬥中更加鋒利。」 | `NgOptimizedImage` (v15) | **設立第二個學習檢查點**。帶領讀者進行第一次的程式碼重構，並引入一個重要的效能優化特性，培養良好的開發習慣。 |

## 第三階段：進階主題與效能優化 (Day 19 - 26)

| 天數 | 主題 | 核心內容規劃 | 個人化切入點 (學徒心聲) | 教授的關鍵特性 (所屬版本) | 融入策略與優化說明 |
| :-- | :--- | :--- | :--- | :--- | :--- |
| **Day 19**| **進階表單：強型別響應式表單 (`FormBuilder`)** | 學習 `FormBuilder` 與強型別表單，用於處理複雜的英雄編輯場景。明確說明其與範本驅動表單的適用場景差異。 | 「從範本驅動表單轉到響應式表單，就像從手排車換到自排車，雖然一開始要適應，但很快就回不去了。尤其在處理複雜路況時。」 | 強型別響應式表單 (v14) | 教授 Angular 表單的「最終形態」，適用於需要複雜驗證、動態變化、且極度要求穩定性的企業級場景。 |
| **Day 20**| **進階表單：`Validators` 與自訂驗證** | 為響應式表單加入內建的 `Validators`，如 `Validators.required`, `Validators.minLength`。學習如何在畫面上顯示對應的錯誤訊息。 | 「規則，是為了保護使用者不犯錯，也是為了保護我們的系統不受汙染。今天，我們來為表單刻上神聖的封印。」 | `Validators` | 任何實用表單的必備功能，是專業開發的基礎。 |
| **Day 21**| **響應式系統：Signals 核心 (`signal`, `computed`, `effect`)** | 基於 Day 6 的經驗，深入講解 `signal`, `computed`, `effect` 的運作原理、使用時機，以及它們為何能帶來更精細的變更偵測。 | 「如果說 Day 6 我們只是感受到了魔法的波動，今天我們就要來解析魔法的咒語。理解它，你就能創造屬於自己的魔法。」 | Signals (v16) | **明確標示難度**。在文章開頭提醒：「**本篇開始進入進階主題，非初學者必須立即完全掌握，但理解其原理能幫助你未來走得更遠。**」 |
| **Day 22**| **響應式系統：`toSignal` (Observable 與 Signal 整合)** | 展示如何使用 `toSignal` 將取得資料的 `Observable` 轉換為 `Signal`，讓元件能以統一的方式處理狀態。 | 「最強的英雄，懂得融合不同的力量。學會讓 Signals 與 RxJS 和諧共舞，你的武器庫將會更加強大。」 | RxJS 互操作性 (v16) | 教授在真實專案中，如何讓新舊兩種響應式系統並存與協作。 |
| **Day 23**| **響應式系統：`resource()` API 展望** | 介紹實驗性的 `resource()` API，並附上「替代方案對照」，展示目前主流如何使用 Signals + RxJS 手動管理非同步狀態。 | 「官方總是在為我們尋找更簡單的道路。`resource()` 就是一個指向未來的路標，讓我們看看，未來的狀態管理可能會有多麼優雅。」 | `resource()` API (v19, 實驗性) | **明確定位**。強調其目的是讓讀者理解 Angular 簡化非同步狀態管理的未來方向，而非當前必學的生產級 API。 |
| **Day 24**| **架構演進：從 `NgModule` 到 Standalone** | 回顧 `NgModule` 的歷史，理解 Standalone Components 的設計哲學，以及它為 Angular 帶來的改變。 | 「這篇文章，獻給所有被 `declarations`, `providers`, `imports`, `exports` 折磨過的 Angular 老兵們。也讓新兵們知道，你們現在有多幸福。」 | 獨立元件 (v14-v15) 的歷史脈絡 | 透過回顧歷史，讓讀者更深刻地理解「為何」要走向 Standalone。 |
| **Day 25**| **效能優化：`@defer` 延遲載入** | 主線聚焦於 `@defer` 的教學與實作。講解其 `on viewport`, `on interaction` 等觸發條件，以及 `placeholder` 和 `loading` 區塊。 | 「`@defer` 就像是學會了影分身之術，只有在需要的時候才召喚本體，大幅提升了我們應用的啟動速度！」 | 可延遲視圖 (`@defer`) (v17) | **聚焦核心**。本章將**主線聚焦於 `@defer` 的教學**。而 v14-v17 的變革，將**作為附錄或資訊圖表**，供讀者快速回顧。 |
| **Day 26**| **第三週總結：進階概念總整理** | 回顧並鞏固響應式表單、Signals、`@defer` 等高難度主題，確保讀者在進入最終章前已融會貫通。 | 「在攀登最終高峰前，讓我們停下腳步，回望來時路。鞏固好每一步，才能站得更穩、看得更遠。」 | 綜合應用 | **設立第三個學習檢查點**。幫助讀者消化吸收本階段的高難度內容，避免帶著疑惑進入最後衝刺。 |

## 第四階段：部署、測試與未來展望 (Day 27 - 30)

| 天數 | 主題 | 核心內容規劃 | 個人化切入點 (學徒心聲) | 教授的關鍵特性 (所屬版本) | 融入策略與優化說明 |
| :-- | :--- | :--- | :--- | :--- | :--- |
| **Day 27**| **部署上線：`ng build` 與靜態網站託管** | 執行 `ng build` 並將應用部署到 GitHub Pages。並在文末附上「更真實的部署管道」簡介，介紹 Firebase Hosting、Vercel、Netlify 等。 | 「分享，是英雄旅程的最終章。今天，我們將把自己的作品部署到全世界都看得到的地方，接受大家的檢驗！」 | `ng build` | 完成應用的最後一哩路，並為讀者的下一步提供實際的部署選擇。 |
| **Day 28**| **效能優化：Zoneless 變更檢測展望** | 補充一個「Zone.js 問題場景」的簡單示例（例如 `setTimeout` 內部的變更檢測問題），再說明 Signals 如何從根本上解決此問題。 | 「Zoneless 就像是從地圖砲（Zone.js）進化到狙擊槍（Signals），它讓我們能更精準、更有效率地更新畫面。這就是 Angular 的未來。」 | Zoneless (v18, 預覽版) | **提供具體痛點**。透過前後對比，讓讀者深刻感受「為何需要 Zoneless」，而不只是空泛的概念。 |
| **Day 29**| **品質保證：單元測試與 E2E 測試** | 1. 服務單元測試。2. 元件單元測試。3. E2E 測試簡介：介紹 Angular 預設的 Playwright，並展示一個簡單的 smoke test。 | 「測試不是麻煩事，而是我們最強的護盾。它讓我們在未來可以安心地揮劍，不必擔心傷到自己。」 | Angular 測試 (Jasmine, Playwright) | **擴大測試廣度**。讓讀者了解完整的測試金字塔概念，建立專業的品質意識。 |
| **Day 30**| **系列總結：回顧與未來之路** | 總結 30 天的學習成果。**加入「未來之路」小節**，簡述 Angular v21 以後的 roadmap。讓讀者感受到這趟旅程是一個新的開始，而非結束。 | 「30 天的旅程即將結束，但真正的英雄之路才正要開始。願你帶著這份地圖，繼續在 Angular 的世界裡探索、成長。」 | 系列總結 | 為整個系列畫上一個勵志且充滿希望的句點，鼓勵讀者持續學習。 |